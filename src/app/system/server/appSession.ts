import { Injectable } from "@angular/core";

@Injectable()
export class AppSession {
    // protected constructor(private cookieService: CookieService) {
    // }
    protected constructor() {
    }
}